# TASKS TO DO

## BACKLOG

[]-When new localsnow ready to take over the domain, you'll need one of:
    - Disable/delete legacy's deploy workflow, or
    - Rename legacy's deploy identifiers (image tag, stack name, secrets prefix, traefik router names) to -legacy variants so both could run side-by-side without clobbering.

## PENDING

## ON PROCESS

## DONE
